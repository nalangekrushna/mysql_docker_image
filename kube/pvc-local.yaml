apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc
  namespace: dev
  labels:
    app: backend-deploy
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 200Mi